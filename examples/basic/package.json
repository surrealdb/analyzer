{
  "name": "@surrealql-analyzer-example/basic",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Vanilla-TS SurrealQL Analyzer example: surrealkit generate -> createClient<Queries> + typed db.query.",
  "scripts": {
    "generate": "surrealkit generate --out src/surrealql-analyzer.d.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@surrealdb/analyzer-client": "workspace:*",
    "surrealdb": "2.0.8"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
