{
  "name": "surrealql-analyzer-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.29.3",
  "description": "Node-side tooling for the SurrealQL Analyzer: the diagnostics catalogue generator and the npx CLI shim",
  "scripts": {
    "docs:diagnostics": "node scripts/gen-diagnostics-page.mjs",
    "docs:diagnostics:check": "node scripts/gen-diagnostics-page.mjs --check && node scripts/gen-diagnostics-page.mjs --verify"
  }
}
