-- 1022: this table was implicitly created (schemaless) by
-- 76_cross_file_order_a.surql's DEFINE FIELD, which sorts before this file —
-- so this plain DEFINE TABLE is a redefinition SurrealDB rejects.
DEFINE TABLE order_probe SCHEMAFULL;
