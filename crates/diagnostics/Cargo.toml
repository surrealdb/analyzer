[package]
name = "surrealql-analyzer-diagnostics"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"
keywords.workspace = true
categories.workspace = true
description = "Structured findings, severities, lint policy, and suppressions for SurrealQL Analyzer"

[dependencies]
serde = { version = "1", features = ["derive"] }
surrealql-analyzer-syntax = { path = "../syntax", version = "0.6.0" }

[lints]
workspace = true
