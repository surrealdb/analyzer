[package]
name = "surrealql-analyzer-macros"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"
keywords.workspace = true
categories.workspace = true
description = "Compile-time checked SurrealQL for Rust — the surql! macro"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
surrealdb-types.workspace = true
surrealql-analyzer-workspace = { path = "../workspace", version = "0.6.0" }
surrealql-analyzer-diagnostics = { path = "../diagnostics", version = "0.6.0" }

[lints]
workspace = true
