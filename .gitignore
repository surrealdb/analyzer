/target
# The type oracle is its own workspace, so it has its own target directory.
/tools/type-oracle/target

.DS_Store

# JavaScript / TypeScript packages
node_modules/
dist/
*.tsbuildinfo
.svelte-kit/
.claude/
